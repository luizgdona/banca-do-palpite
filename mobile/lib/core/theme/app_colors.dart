import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Base
  static const cream = Color(0xFFF5EEDC);
  static const green = Color(0xFF124832);
  static const greenMid = Color(0xFF1E6444);
  static const greenLight = Color(0xFF2D8255);

  // Acentos
  static const amber = Color(0xFFDC8C14);
  static const amberLight = Color(0xFFF0AA32);

  // Status
  static const liveBadge = Color(0xFFB43728);
  static const winColor = Color(0xFF00C853);
  static const exactColor = Color(0xFFFFD700);
  static const lossColor = Color(0xFF6B7B6B);

  // Texto
  static const offWhite = Color(0xFFFCF8EE);
  static const darkText = Color(0xFF161E16);
  static const mutedText = Color(0xFF7A9A7A);
  static const mutedDark = Color(0xFF8A8A7A);

  // Extras
  static const inputFill = Color(0xFFEDE4CC);
  static const divider = Color(0xFFD4C9A8);
}
